1. Change the mondule interface so that it implements an interface ...
	Init function implements an ineterface!
	At run time check if it is just a function or an interafce pointer
	If it is just a "function" then do the standard fuction , then wrap it and repalce it with a interface.
	If it is an interface to our stuff then do 







































	"Decimals":"<no value>",
	"FullName":"test01 llc",
