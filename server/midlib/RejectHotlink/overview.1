
PreventHotlink -- Middleware ( www.name.com, name.com etc )
	-- Check referer for ... in set
	Prevent image hotlinking
	return error, return page based on MIME type?
	Allowed URLs - like req.Host == "localhost:*"

http://www.yourhtmlsource.com/sitemanagement/bandwidththeft.html

Remember to allow/disallow empty Referer header
