RestQL - Query language spanning SQL and Entity Relationship Models
===================================================================

Basic Query
	{
		from: { url:/api/table/someTable
			, username: Name
			, ...
			}
		where:
		order_by:
		having:
		group_by:
		project:
		server: {
		}
	}
